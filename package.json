{
    "name": "amplify-app",
    "version": "0.0.0",
    "private": true,
    "scripts": {
      "start": "http-server -o",
      "build": "echo 'No build needed for plain HTML/CSS/JS.'"
    },
    "dependencies": {
      "http-server": "^14.0.0"
    }
  }